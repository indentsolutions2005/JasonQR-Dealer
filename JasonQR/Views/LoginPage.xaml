<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="JasonQR.Views.LoginPage"
             xmlns:controls="clr-namespace:JasonQR"
             xmlns:local="clr-namespace:jasonQR"
             NavigationPage.HasNavigationBar="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Image Grid.Row="0" Grid.Column="0" Source="{local:EmbeddedImage JasonQR.Images.loginBg.png}" Aspect="AspectFit" />
        <Grid Padding="20" RowSpacing="20" VerticalOptions="CenterAndExpand">
            <Grid.RowDefinitions>
                <RowDefinition Height="90" />
                <RowDefinition Height="45" />
                <RowDefinition Height="45" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackLayout Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Spacing="20">
                <Label Text="Please enter your phone number to enable this service" TextColor="Black" FontSize="Medium"/>
                <Frame  HasShadow="False" BorderColor="Gray" CornerRadius="10">
                    <Frame.Padding>
                        <OnPlatform x:TypeArguments="Thickness" iOS="10" Android="0"/>
                    </Frame.Padding>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <controls:CustomEntry HeightRequest="45" Text="{Binding PhoneNumber}" Placeholder="Phone Number" Grid.Column="0"/>
                    </Grid>
                </Frame>
            </StackLayout>
            <Button Grid.Row="1" Grid.Column="0" Command="{Binding LoginCommand}" BackgroundColor="DarkRed" FontAttributes="Bold" Grid.ColumnSpan="2" FontSize="Large"  HorizontalOptions="FillAndExpand" Text="Verify Phone Number" TextColor="White"/>
            <Label Grid.Row="2" Grid.Column="0" TextColor="Black" FontSize="Small" Grid.ColumnSpan="2" HorizontalOptions="FillAndExpand" Text="By tapping Verify Phone Number, An sms will be sent to verify"/>
        </Grid>
        <StackLayout IsVisible="{Binding IsBusy}" Grid.Row="0" Grid.Column="0" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" BackgroundColor="#90000000">
          <ActivityIndicator IsRunning="True" Color="White" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
        </StackLayout>
    </Grid>

</ContentPage>
